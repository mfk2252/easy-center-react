rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // وضع التطوير: السماح لأي مستخدم مسجل بالدخول بقراءة وكتابة البيانات
      allow read, write: if request.auth != null;
    }
  }
}
